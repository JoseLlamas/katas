function transformTree (tree: number[]): any {
  let response = null
  if (tree.length >= 1) {
    response = {}
  }
  for (let i = 1; i < tree.length; i++) {
    const elm = tree[i]
    if (elm !== null) {

    }
  }
  return response
}

export { transformTree }
